# 六层解耦架构总览（Web 版）

> 配套 PNG：`图片/架构图-六层解耦.png`（可放入 PPT）

```mermaid
flowchart TB
    classDef user fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#1b5e20
    classDef gate fill:#e0f2f1,stroke:#00695c,stroke-width:2px,color:#004d40
    classDef biz  fill:#fff8e1,stroke:#f9a825,stroke-width:2px,color:#5d4037
    classDef ai   fill:#ede7f6,stroke:#5e35b1,stroke-width:2px,color:#311b92
    classDef ext  fill:#fce4ec,stroke:#ad1457,stroke-width:2px,color:#4a148c
    classDef inf  fill:#eceff1,stroke:#455a64,stroke-width:2px,color:#263238

    subgraph CH["用户渠道层 · Web 单入口"]
        WEB["浏览器 Web<br/>15 个静态页面 · 响应式"]
    end

    subgraph GT["接入层 · 统一请求入口"]
        direction TB
        G1["WebAuthInterceptor<br/>Session 鉴权 · 白名单"]
        G2["REST Controllers<br/>auth / care / shop / community / ai"]
        G3["PageController<br/>页面转发"]
    end

    subgraph BZ["业务应用层 · 全场景覆盖"]
        direction TB
        B1["智能护理 · 病害诊断<br/>care · aicare · disease"]
        B2["商城 · 库存<br/>ebusiness · inventory"]
        B3["社区 · 时间线<br/>community · timeline"]
        B4["简报 · 推送 · 账号<br/>briefing · push · auth"]
        B5["提醒 · 知识库<br/>reminder · kb"]
    end

    subgraph AI["AI 核心能力层 · 技术心脏"]
        direction TB
        A1["对话引擎<br/>SpringAiChatService · LlmService"]
        A2["MessageRouter<br/>意图路由 · 关键词兜底"]
        A3["ToolCallingService<br/>68 个 @Tool · 5 轮循环"]
        A4["AgentService（保留）<br/>8 个 BaseTool · 120s 超时"]
        A5["记忆与 RAG<br/>历史 · 摘要 · 向量检索"]
    end

    subgraph EX["外部服务层 · 能力集成底座"]
        direction TB
        E1["DeepSeek V4-Pro<br/>对话模型"]
        E2["阿里云百炼<br/>Embedding · qwen-vl · qwen-image"]
        E3["讯飞 TTS<br/>语音合成"]
        E4["高德 · 心知天气 · 百度<br/>导航 · 天气 · 搜索"]
        E5["WebPush<br/>浏览器推送"]
    end

    subgraph IF["基础设施层 · 底层支撑"]
        direction TB
        I1["MySQL ilink_chat<br/>业务表 · 自动建表"]
        I2["SQLite rag_knowledge<br/>向量知识库"]
        I3["uploads<br/>文件存储"]
        I4["阿里云服务器<br/>Java 21 · systemd · ffmpeg"]
    end

    WEB --> G1
    WEB --> G2
    WEB --> G3
    G2 --> A1
    G2 --> A2
    A2 --> A3
    A1 --> A5
    A3 --> A1
    A3 --> A4
    A4 --> E1
    A3 --> E1
    A3 --> E2
    A3 --> E3
    A3 --> E4
    A5 --> E2
    A5 --> I2
    A5 --> I1
    B1 --> A1
    B1 --> A2
    B1 --> I1
    B2 --> I1
    B3 --> I1
    B4 --> I1
    B5 --> I1
    B4 --> E5
    B1 --> I3
    I1 --> I4
    I2 --> I4
    I3 --> I4

    class WEB user
    class G1,G2,G3 gate
    class B1,B2,B3,B4,B5 biz
    class A1,A2,A3,A4,A5 ai
    class E1,E2,E3,E4,E5 ext
    class I1,I2,I3,I4 inf
```

## 各层说明

| 层 | 说明 |
|---|---|
| 用户渠道层 | 浏览器 Web 单入口，15 个静态页面覆盖登录注册、首页、对话、护理、病害、提醒、知识库、简报、商城、库存、社区、时间线，移动端响应式适配。 |
| 接入层 | WebAuthInterceptor 统一鉴权（`/api/**`、`/uploads/**` 需登录）+ REST Controllers + PageController 页面转发，实现请求统一预处理。 |
| 业务应用层 | 智能护理、病害诊断、商城、库存、社区、时间线、简报、推送、账号、提醒、知识库 10 余个业务模块，覆盖养护咨询到周边消费、社交分享全链路。 |
| AI 核心能力层 | SpringAiChatService 对话引擎 + ToolCallingService（68 个 @Tool）为主通路，AgentService（8 个 BaseTool）保留沉淀；意图路由、记忆与 RAG 检索构成智能调度中枢。 |
| 外部服务层 | 集成 DeepSeek V4-Pro、阿里云百炼、讯飞、高德、心知天气、百度、WebPush，按需调用大模型、视觉、生图、语音、天气、地图、搜索、推送等第三方能力。 |
| 基础设施层 | MySQL + SQLite 向量库双数据源，uploads 文件存储，阿里云服务器（Java 21 / systemd / ffmpeg）承载；启动自动建表，无需手工运维 SQL。 |
