# 周报（2026-08-03 ~ 2026-08-08）

> 历史文档：记录当周状态。文中提到的微信端能力已于 2026-08-15 随微信 ILink / 公众号接入一并移除，项目现为纯 Web 端；WebPush 服务器端问题也已修复上线。

## 一、本周工作内容

### 1. 功能开发

- **护理提醒真实送达**：提醒到期后真正推送给用户——微信用户直接收微信消息，网页用户收 WebPush，不再只是改状态
- **首页待办数字**：新增待办提醒统计接口，首页数据卡实时显示 PENDING 数量
- **启动自动建表**：新增 `CareSchemaInitializer`，启动自动执行建表脚本，解决 `care_reminder` 表缺失问题，服务器零手工 SQL
- **视觉/生图接口修复**：qwen-vl、qwen-image 请求从错误的 DeepSeek 端点迁移到阿里云百炼兼容接口，修复"图片识别 400"问题

### 2. 代码优化与工程治理

- **敏感文件治理**：停止跟踪 uploads、tmp、login_state、query，完善 .gitignore（P0-2）
- **配置外置**：SQLiteConfig 数据库凭据改为读取配置，不再硬编码在 Java 代码
- **资源治理**：多个线程池补充 `@PreDestroy` 优雅关闭；定时任务线程池扩容（3 线程）
- **缺陷修复**：图片 URL 不再写死 localhost、JPQL LIKE 写法修正、`System.out.println` 替换为 logger
- **构建修复**：Lombok 注解处理器配置补齐，命令行 `mvn compile/package` 可正常执行

### 3. 前端优化

- 登录/注册/聊天/商城共享样式全面重写（app.css / shop.css v2）
- 首页重设计：Hero 问候 + 数据卡 + 核心系统 + 特色功能 + 推送开关
- 新增 Liquid Glass 玻璃拟态落地页（登录前展示，已登录自动跳首页）
- 浏览器推送改为手动开关订阅，不再页面加载即弹授权

### 4. 部署与发布

- 阿里云服务器完成新版本部署（systemd `ilink.service`），含备份、灰度切换、回滚预案
- 服务器数据库补齐 `identify_history` 缺失列，消除启动建索引报错
- GitHub 仓库 main 分支完成完整覆盖更新，旧内容已替换，敏感文件不再入库

### 5. 文档与汇报材料

- `落地文档/` 新增/更新 10+ 份文档：项目介绍、项目知识文档、Java 学习指南、记忆 RAG 设计、双端功能介绍、Agent 工具调用体系、Agent 核心能力设计介绍等
- 产出汇报用图：架构图（总览/六层解耦/纵向/定制版）、Agent 执行引擎、Agent 工具调用体系等高清 PNG
- 完成 12 页项目汇报 PPT（`项目汇报.pptx`）

## 二、本周成果数据

| 类别 | 数量 |
|---|---|
| 新增/更新文档 | 10+ 份 |
| 汇报用高清图 | 6+ 张 |
| 汇报 PPT | 12 页 |
| 代码优化项 | 10+ 处 |
| 服务器部署 | 2 次（上线 + 修复版） |
| 接口新增 | 1 个（待办统计） |

## 三、问题与风险

- **WebPush 服务器端不可用**：BouncyCastle/密钥报错，浏览器推送被禁用（遗留问题，待排查）
- **GitHub 历史含旧密钥**：历史清理与密钥轮换仍挂起，需排期处理
- **已知功能缺口**：微信端"回复已完成"闭环待完善；多用户数据隔离缺失；RAG 检索未按会话隔离；微信消息批量处理仅取第一条
- **密钥策略提醒**：本地 `application.properties` 当前含明文密钥，后续推送仓库前需确认策略

## 四、下周计划

1. 排查并修复服务器 WebPush 推送问题
2. 完成微信端"已完成"提醒闭环，将护理域工具下沉到微信通路
3. 推进 SSE 流式对话，提升聊天体验
4. 多用户数据隔离改造（档案归属用户）
5. 知识库管理后台 / 每日简报页面
